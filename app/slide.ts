import { Component, Input } from '@angular/core';

@Component({
    selector: 'slide',
    template: `
  `
})
export class Slide {
    @Input() title = '';

    onSelect() {
        //TODO: add code
    }

    onDeSelect() {
        //TODO: add code
    }
}
